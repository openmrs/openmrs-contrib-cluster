{{- if .Values.monitoring.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: logs-dashboard
  labels:
    grafana_dashboard: "1"
    {{- include "openmrs-backend.labels" . | nindent 4 }}
data:
  logs-dashboard.json: |
{{ .Files.Get "dashboards/logs-dashboard.json" | indent 4 }}
{{- end }}