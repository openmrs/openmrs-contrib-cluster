apiVersion: v1
kind: Service
metadata:
  name: {{ include "openmrs.fullname" . }}
  labels:
    {{- include "openmrs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openmrs.selectorLabels" . | nindent 4 }}
